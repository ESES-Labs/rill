name: Deploy Compose Stack

on:
  push:
    branches:
      - main
    paths:
      - 'rill-backend/**'
      - 'docker-compose.yaml'
      - 'docker-compose.override.yaml'
  workflow_dispatch:
    inputs:
      run_compose_up:
        description: Run VPS deploy once. Requires RILL_DEPLOY_* secrets in GitHub Actions.
        required: true
        type: boolean
        default: false
  workflow_call:
    inputs:
      run_compose_up:
        required: false
        type: boolean
        default: false
    secrets:
      RILL_DEPLOY_HOST:
        required: false
      RILL_DEPLOY_USER:
        required: false
      RILL_DEPLOY_PORT:
        required: false
      RILL_DEPLOY_PATH:
        required: false
      RILL_DEPLOY_SSH_KEY:
        required: false
      RILL_GHCR_TOKEN:
        required: false

permissions:
  contents: read
  actions: read

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy Compose Stack
    if: >-
      (github.event_name == 'workflow_dispatch' && inputs.run_compose_up) ||
      github.event_name == 'workflow_call' ||
      github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: production
    outputs:
      deployed: ${{ steps.gate.outputs.deploy }}
    env:
      DEPLOY_HOST: ${{ vars.RILL_DEPLOY_HOST || secrets.RILL_DEPLOY_HOST }}
      DEPLOY_USER: ${{ vars.RILL_DEPLOY_USER || secrets.RILL_DEPLOY_USER }}
      DEPLOY_PORT: ${{ vars.RILL_DEPLOY_PORT || secrets.RILL_DEPLOY_PORT || '22' }}
      DEPLOY_PATH: ${{ vars.RILL_DEPLOY_PATH || secrets.RILL_DEPLOY_PATH }}
      DEPLOY_SSH_KEY: ${{ secrets.RILL_DEPLOY_SSH_KEY }}
      GHCR_TOKEN: ${{ secrets.RILL_GHCR_TOKEN }}
      GHCR_USER: ${{ github.actor }}
      AUTO_DEPLOY: ${{ vars.RILL_COMPOSE_AUTO_DEPLOY || 'false' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed deploy surfaces
        id: changes
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
          BEFORE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -Eeuo pipefail

          need_backend=false
          need_compose=false

          if [[ "${BEFORE_SHA}" =~ ^0+$ ]]; then
            files=$(gh api "/repos/${GITHUB_REPOSITORY}/commits/${HEAD_SHA}" --jq '.files[].filename')
          else
            files=$(gh api "/repos/${GITHUB_REPOSITORY}/compare/${BEFORE_SHA}...${HEAD_SHA}" --jq '.files[].filename')
          fi

          printf 'Changed files for %s:\n%s\n' "${HEAD_SHA}" "${files}"

          while IFS= read -r file; do
            [ -n "${file}" ] || continue
            case "${file}" in
              rill-backend/*)
                need_backend=true
                ;;
              docker-compose.yaml|docker-compose.override.yaml)
                need_compose=true
                ;;
            esac
          done <<< "${files}"

          printf 'need_backend=%s\n' "${need_backend}" >> "${GITHUB_OUTPUT}"
          printf 'need_compose=%s\n' "${need_compose}" >> "${GITHUB_OUTPUT}"
          printf 'Deploy requirements: backend=%s compose=%s\n' "${need_backend}" "${need_compose}"

      - name: Wait for backend image build
        if: github.event_name == 'push' && steps.changes.outputs.need_backend == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -Eeuo pipefail

          for attempt in $(seq 1 80); do
            runs=$(gh api \
              "/repos/${GITHUB_REPOSITORY}/actions/workflows/build-backend.yaml/runs?branch=main&event=push&per_page=50")

            success_count=$(jq '[.workflow_runs[] | select(.head_sha == env.HEAD_SHA and .status == "completed" and .conclusion == "success")] | length' <<< "${runs}")
            failed_count=$(jq '[.workflow_runs[] | select(.head_sha == env.HEAD_SHA and .status == "completed" and (.conclusion == "failure" or .conclusion == "cancelled" or .conclusion == "timed_out"))] | length' <<< "${runs}")

            if [ "${success_count}" -gt 0 ]; then
              printf 'Build Backend Image is successful for %s.\n' "${HEAD_SHA}"
              break
            fi

            if [ "${failed_count}" -gt 0 ]; then
              printf '::error::Build Backend Image failed for %s. Skipping compose deploy.\n' "${HEAD_SHA}"
              exit 1
            fi

            printf 'Waiting for Build Backend Image for %s (%s/80).\n' "${HEAD_SHA}" "${attempt}"
            sleep 15
          done

      - name: Check compose deploy gate
        id: gate
        env:
          FORCE_DEPLOY: ${{ inputs.run_compose_up }}
        run: |
          set -Eeuo pipefail

          auto_deploy=$(printf '%s' "${AUTO_DEPLOY}" | tr '[:upper:]' '[:lower:]' | tr -d '[:space:]')

          if [ "${FORCE_DEPLOY}" = "true" ] || [ "${auto_deploy}" = "true" ]; then
            printf 'deploy=true\n' >> "${GITHUB_OUTPUT}"
            printf 'Compose deploy gate is open.\n'
          else
            printf 'deploy=false\n' >> "${GITHUB_OUTPUT}"
            printf 'Compose deploy gate is closed. Skipping VPS deployment.\n'
          fi

      - name: Validate deployment configuration
        if: steps.gate.outputs.deploy == 'true'
        run: |
          missing=()
          [ -n "${DEPLOY_HOST}" ] || missing+=("RILL_DEPLOY_HOST")
          [ -n "${DEPLOY_USER}" ] || missing+=("RILL_DEPLOY_USER")
          [ -n "${DEPLOY_PATH}" ] || missing+=("RILL_DEPLOY_PATH")
          [ -n "${DEPLOY_SSH_KEY}" ] || missing+=("RILL_DEPLOY_SSH_KEY")

          if [ "${#missing[@]}" -ne 0 ]; then
            printf '::error::Missing VPS deployment configuration: %s\n' "${missing[*]}"
            exit 1
          fi

      - name: Configure SSH
        if: steps.gate.outputs.deploy == 'true'
        run: |
          install -d -m 0700 ~/.ssh
          printf '%s\n' "${DEPLOY_SSH_KEY}" > ~/.ssh/rill_deploy_key
          chmod 0600 ~/.ssh/rill_deploy_key
          ssh-keyscan -p "${DEPLOY_PORT}" -H "${DEPLOY_HOST}" >> ~/.ssh/known_hosts

      - name: Log in to GHCR on host
        if: steps.gate.outputs.deploy == 'true' && env.GHCR_TOKEN != ''
        run: |
          printf '%s' "${GHCR_TOKEN}" \
            | ssh -i ~/.ssh/rill_deploy_key -p "${DEPLOY_PORT}" "${DEPLOY_USER}@${DEPLOY_HOST}" \
                "docker login ghcr.io -u '${GHCR_USER}' --password-stdin"

      - name: Deploy via Docker Compose
        if: steps.gate.outputs.deploy == 'true'
        env:
          NEED_BACKEND: ${{ steps.changes.outputs.need_backend }}
          NEED_COMPOSE: ${{ steps.changes.outputs.need_compose }}
        run: |
          remote_env=$(
            printf 'RILL_DEPLOY_PATH=%q GITHUB_EVENT_NAME=%q NEED_BACKEND=%q NEED_COMPOSE=%q' \
              "${DEPLOY_PATH}" "${GITHUB_EVENT_NAME}" "${NEED_BACKEND:-false}" "${NEED_COMPOSE:-false}"
          )

          ssh -i ~/.ssh/rill_deploy_key -p "${DEPLOY_PORT}" "${DEPLOY_USER}@${DEPLOY_HOST}" \
            "${remote_env} bash -s" <<'REMOTE'
          set -Eeuo pipefail

          cd "${RILL_DEPLOY_PATH}"

          git pull --ff-only

          test -f docker-compose.yaml
          test -f rill-backend/.env

          if [ "${GITHUB_EVENT_NAME}" != "push" ]; then
            NEED_BACKEND=true
            NEED_COMPOSE=true
          fi

          docker compose config --quiet

          if [ "${NEED_BACKEND}" = "true" ] || [ "${NEED_COMPOSE}" = "true" ]; then
            docker compose pull rill-backend
            docker compose up -d --force-recreate --no-deps rill-backend
          else
            printf 'No services need recreation for this deploy.\n'
          fi

          docker compose ps rill-backend
          REMOTE
