{
  "name": "rill",
  "private": true,
  "workspaces": ["rill-backend", "packages/*"],
  "scripts": {
    "dev:backend": "bun run --filter rill-backend dev",
    "build:sdk": "bun run --filter @rill/sdk build",
    "check:sdk": "bun run --filter @rill/sdk check"
  }
}
